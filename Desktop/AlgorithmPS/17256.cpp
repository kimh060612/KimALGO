#include <iostream>

using namespace std;

int main()
{
    int x, y, z;
    int u, v, w;
    cin >> x >> y >> z;
    cin >> u >> v >> w;
    cout << u - z << " " << v / y << " " << w - x << endl;
    return 0;
}